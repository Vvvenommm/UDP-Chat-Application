
TEST

1) Erst 2 Server nacheinander starten (erst .24, dann .21 - also höhere IP zuerst) , dann 2 Clients hinzufügen
- Server erkennen sich, haben sich auf einen Leader geeinigt
- 1. Client beim NICHT Leader PC hinzufügen
FEHLER: 
a) Execption in thread Trhead-2 (start_receiver) im Server: self_run:
data, address = multicas_socket.recvfrom(1024) unter multicast_server 
b) gleichzeitg im Nicht-Leader-Server: Vorahnden eVerbindung vom Remotehost geschlossen
 - stimmt aber nicht
c) Client findet den Nicht-Leader-Server als Leader und kommuniziert mit ihm 
- Nachricht falsch ausgegeben, aber richtig interpretiert: Leader wäre .24, aber 2x (data + address .21 empfangen)

- 2. Client auf ursprünglich Leader PC starten 
FEHLER: findet auch falschen PC als leader

- 3. Crashen des Leaders - hier, wo alle clients verbunden sind 
> wird erkannt, Leader Election wird durchgeführt bzw. erkannt dass alleine
> Client Liste leer 
FEHLER: Wenn Clients schreiben: eine Verbindung vom Remotehost geschlossen,
es wurde keien Verbindung zum Client aufgebaut

NICHT OPTIMAL: Keine Meldung beim Client

ALTERNATIVE:
- 3. Crashen des ursprünglichen Leaders:
> Crash wird erkannt vom neuen inoffiziellen Leader
FEHLER:
a) start_heartbeas_listener data, addr. = heartbeat_socket.recvfrom(1024)
b) self run 
c) möchte den gecrashten Leader aus Liste entfernen, aber der Leader war gar nie
in der Liste drin 
POSITIV: Verbindung zu den Clients ist immer noch da; können kommunizieren

4. Client gegangen
NICHT OPTIMAL: 
Keine Nachricht/ Anzeige beim Server oder beim Client

5. Zweiter Server wieder hinzufügen
> Leader election wurde durchgeführt
> er wurde neuer Leader
> vom 2. Server erkannt, aber nicht vom Client: Client hat keine Verbindung
mehr zu irgendeinem da Client_List = leer

------------------------------------------------------------------

TEST

1) Erst 2 Server nacheinander starten (erst .21, dann .24 - also niedrige IP zuerst) , dann 2 Clients hinzufügen
- Server erkennen sich, haben sich auf einen Leader geeinigt
- 1. Client beim NICHT Leader PC hinzufügen
> Client findet den richtigen Leader und kann kommunizieren
FEHLER: 
a) Execption in thread Trhead-2 (start_receiver) im Server: self_run:
data, address = multicas_socket.recvfrom(1024) unter multicast_server 
VERBINDUNG ABER TROTZDEM DA
# Auch die Fehlermeldung, wenn Client beim Leader PC hinzugefügt wird

- 2. Client auf Leader PC starten 
> Finder richtiger Leader, fkt!
> kann kommunizieren, Leader bekommt die Nachrichten, anderer Client auch
> Heartbeat fkt. auch auf Nicht-Leader Server

- 3. Crashen des Leaders 
> wird erkannt, Leader Election durchgeführt wird bzw. erkannt dass einziger Server
> Client Liste leer 
FEHLER: 
a) Wenn Clients schreiben: eine Verbindung vom Remotehost geschlossen,
es wurde keien Verbindung zum Client aufgebaut
ABER: wenn Client neu verbunden wird fkt. es

- 4. Client geht 
FEHLER: wird nicht beim Server angezeigt 

------------------------------------------------------------------------

Änderungen:
leader-election else: 
utils.new_leader = utils.myIP
